apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: go-auto-claim0
  name: go-auto-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
